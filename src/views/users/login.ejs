<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

    <body>

        <%- include('../partials/header')%>
    
        <hr>
        
        <% if (typeof errors != 'undefined') { %>
         <ul>
           <% for(error of errors) { %> 
            <%= error.msg %> 
            <% } %> 
         </ul>
        <% } %>

          <h3 class='title-form'>Inciar sesión</h3>
        <h4 class='comunidad'>¿Aún no tienes una cuenta? <a href="/users/register">Unite a la comunidad</a></h4>
        <main class="login-form">
            <form>
              <div class="form-name">
              <input type="email" name="email" placeholder="Email">
            </div>

            <div class="form-pass">
              
              <input type="password" name="password" placeholder="Contraseña">
            </div>

            <div>
              <label for="remb" style="margin-left: 20px; margin-right: -20px;">Recordarme</label>
              <input type="checkbox" id="remb" name="remember" style="max-width: 100px;">
            </div>

            <br>
            <div class="form-btn-send">
              <button type="submit">Ingresar</button>
            </div>
            </form>
           
          </main>
          <!-- <div>
            <ul>
          <li>Terms</li>
          <li>Privacy</li>
          <li>Security</li>
          <li>Terms</li>
        </ul> -->
        </div>
    </body>

</html>