<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head') %>

    <body>

        <%- include('../partials/header')%>
    
        <hr>
        
        <% if (typeof errors != 'undefined') { %>
          <p style="color: red">ERROR</p>
         <ul>
           <% for(error of errors) { %> 
            <li><%= error.msg %></li> 
            <% } %> 
         </ul>
        <% } %>

          <h3 class='title-form'>Inciar sesión</h3>
        <h4 class='comunidad'>¿Aún no tienes una cuenta? <a href="/users/register">Unite a la comunidad</a></h4>
        <main class="login-form">
            <form action="/users/login" method="POST">
              <div class="form-email">
              <input type="email" name="email" placeholder="Email">
            </div>

            <div class="form-pass">
              
              <input type="password" name="password" placeholder="Contraseña">
            </div>

            <div class="form-cookie">
              <label for="remb">Recordame</label>
              <input type="checkbox" id="remb" name="remember">
            </div>

            <br>
            <div class="form-btn-send">
              <button type="submit">Ingresar</button>
            </div>
            </form>
           
          </main>
       
          <%- include('../partials/footer') %> 

    </body>

</html>